body {
    background-color: #232525;
    font-family: Arial, Helvetica, sans-serif;
    justify-content: center;
    align-items: center;
    color: #7CCFF5;
    height: 698px;
    display: flex;
}

*{
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    outline: none;
}

.container {                     /* CONTENUTO */
    background-color: #232525;
    border-radius: 5px;
    width: 400px;
    padding: 30px 40px;
    border: 3px solid #7CCFF5;
}

.container h1 {  /* CONTENUTO H1 */
    text-align: center;
    margin: 0 0 20px;
    font-size: 24px;
}

.input {                 /* STILE INPUT DI TESTO */
    margin-bottom: 20px;
    position: relative;
}

.input label {
    display: block;
    margin-bottom: 10px;
}

.input input {
    width: 100%;
    height: 40px;
    padding: 0 15px;
    background-color: transparent;
    border: 2px solid #7CCFF5;
    border-radius: 5px;
    color: #7CCFF5;
}

.input small {
    display: block;
    color: #7CCFF5;
    font-weight: bold;
    font-size: 10px;
    padding-top: 3px;
    visibility: hidden;
}

.input.success input {             /* STILE INPUT DI TESTO DATI CORRETTI */
    border: 2px solid #2ECC71;
}

.input.error input {              /* STILE INPUT DI TESTO DATI ERRATI O MANCANTI */
    border: 2px solid #E74C3C;
}

.input.error small {
    visibility: visible;
}

button {                /* STILE PULSANTE */
    width: 100%;
    height: 40px;
    line-height: 40px;
    background-color: #232525;
    border: 3px solid #7CCFF5;
    border-radius: 2px;
    cursor: pointer;
    color: #7CCFF5;
    display: block;
    font-size: 16px;
}

.message a:link {   /* COLORE LINK NON VISITATO */
    color: rgb(12, 244, 252);
}
                       /* COLORE LINK VISITATO */
.message a:visited {
    color: rgb(255, 81, 0);
}

    
